

# Обзор трёх популярных библиотек для машинного обучения и нейронных сетей:  **TensorFlow** , **Keras** и  **PyTorch** .

---

## 🧠 1. **TensorFlow** (от Google)

### 📌 Описание:

TensorFlow — это **мощный фреймворк от Google** для построения и обучения моделей машинного обучения. Поддерживает и low-level, и high-level API.

### ✅ Особенности:

* Поддержка **CPU, GPU и TPU**
* Подходит для **продакшн и мобильных** приложений
* Использует **тензоры и графы вычислений**
* Имеет библиотеку `tf.keras` для упрощённой разработки

### 🔧 Пример:

```python
import tensorflow as tf
from tensorflow.keras import layers

model = tf.keras.Sequential([
    layers.Dense(64, activation='relu'),
    layers.Dense(1, activation='sigmoid')
])
model.compile(optimizer='adam', loss='binary_crossentropy')
model.fit(X_train, y_train, epochs=5)
```

---

## 🔧 2. **Keras** (входит в TensorFlow)

### 📌 Описание:

Keras — это **высокоуровневая оболочка** для создания нейросетей. Ранее была самостоятельной, но теперь полностью интегрирована в TensorFlow как `tf.keras`.

### ✅ Особенности:

* Очень **простой и читаемый синтаксис**
* Идеальна для **прототипирования**
* Работает на базе TensorFlow (или других backend’ов в старых версиях)

---

## 🔥 3. **PyTorch** (от Facebook/Meta)

### 📌 Описание:

PyTorch — это  **гибкий и «питоничный» фреймворк** , активно используемый в научной и исследовательской среде.

### ✅ Особенности:

* Динамические вычисления (eager execution)
* Удобная отладка, как в обычном Python
* Используется HuggingFace, OpenAI, Meta и др.
* Прост в обучении и отладке

### 🔧 Пример:

```python
import torch
import torch.nn as nn
import torch.optim as optim

class Net(nn.Module):
    def __init__(self):
        super().__init__()
        self.fc = nn.Linear(10, 1)

    def forward(self, x):
        return torch.sigmoid(self.fc(x))

model = Net()
criterion = nn.BCELoss()
optimizer = optim.Adam(model.parameters())

output = model(torch.randn(1, 10))
```

---

## 📊 Сравнение

| Характеристика                  | **TensorFlow (tf.keras)**                    | **Keras (отдельно)** | **PyTorch**                  |
| --------------------------------------------- | -------------------------------------------------- | ---------------------------------- | ---------------------------------- |
| Уровень API                            | Высокий + низкий                      | Только высокий        | Высокий + низкий      |
| Простота использования   | 👍                                                 | 👍👍                               | 👍 (больше контроля) |
| Популярность в продакшн  | ✅✅✅                                             | ❌                                 | ✅                                 |
| Подходит для обучения      | ✅                                                 | ✅                                 | ✅✅                               |
| Динамическое выполнение | По умолчанию — нет, но можно | Да                               | ✅ по умолчанию         |
| Используется HuggingFace          | ❌                                                 | ❌                                 | ✅                                 |

---

## 🏁 Какую библиотеку выбрать?

| Цель                                            | Рекомендация        |
| --------------------------------------------------- | ------------------------------- |
| Быстрый старт / обучение        | **Keras или tf.keras** |
| Гибкость и исследования        | **PyTorch**               |
| Вывод в продакшн / мобильные | **TensorFlow**            |
| Работа с языковыми моделями | **PyTorch + HuggingFace** |

---


Активационные функции — это  **ключевой элемент нейронных сетей** , который добавляет **нелинейность** в модель. Без них нейронная сеть вела бы себя как обычная линейная регрессия, независимо от числа слоев.

---

## 📌 Зачем нужны активационные функции?

* Позволяют сети **обучаться сложным зависимостям**
* Делают возможным **обобщение** и **глубокое обучение**
* Позволяют использовать **градиентный спуск**

---

## 🔧 Основные активационные функции

| Название     | Формула                                                         | Особенности                                                                                                            |
| -------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| **Sigmoid**    | σ(x)=11+e−x\sigma(x) = \frac{1}{1 + e^{-x}}                          | Выход от 0 до 1. Хороша для вероятностей. Может вызывать**vanishing gradient** . |
| **Tanh**       | tanh⁡(x)=ex−e−xex+e−x\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}  | От -1 до 1. Центрирована. Могут исчезать градиенты.                                         |
| **ReLU**       | f(x)=max⁡(0,x)f(x) = \max(0, x)                                       | Быстрая, популярная. Проблема «мертвых нейронов».                                       |
| **Leaky ReLU** | f(x)=max⁡(0.01x,x)f(x) = \max(0.01x, x)                               | Альтернатива ReLU. Избегает «мертвых нейронов».                                              |
| **ELU**        | f(x)=xf(x) = xifx>0x>0, elseα(ex−1)\alpha(e^x - 1)                   | Плавнее, но дороже по вычислениям.                                                                    |
| **Softmax**    | softmax(xi)=exi∑exj\text{softmax}(x_i) = \frac{e^{x_i}}{\sum e^{x_j}} | Используется на выходном слое для многоклассовой классификации.           |

---

## 📉 Визуализация (словами)

* **Sigmoid** : S-образная кривая, значения от 0 до 1
* **Tanh** : как sigmoid, но от -1 до 1
* **ReLU** : 0 для отрицательных x, x — для положительных
* **Leaky ReLU** : как ReLU, но с небольшим наклоном слева
* **Softmax** : распределяет "уверенность" по всем классам, сумма = 1

---

## 🧠 Применение по слоям

| Где используется                                      | Какая функция                     |
| -------------------------------------------------------------------- | --------------------------------------------- |
| Скрытые слои                                              | ReLU, LeakyReLU, Tanh                         |
| Выход (бинарная классификация)             | Sigmoid                                       |
| Выход (многоклассовая классификация) | Softmax                                       |
| Выход (регрессия)                                      | Нет функции или линейная |

---

## 🔍 Пример в PyTorch

```python
import torch.nn as nn

model = nn.Sequential(
    nn.Linear(10, 20),
    nn.ReLU(),
    nn.Linear(20, 1),
    nn.Sigmoid()
)
```

---

## 📦 В TensorFlow / Keras

```python
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense

model = Sequential([
    Dense(64, activation='relu'),
    Dense(1, activation='sigmoid')
])
```
